# Nixpacks configuration for Python Pyramid app
providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -e ."]

[start]
cmd = "pserve production.ini"
